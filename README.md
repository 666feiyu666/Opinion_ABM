# Opinion Model — Null Scenario

The `null` branch implements the matched OLIM 2.0 control scenario. It uses the
shared Beta-belief, message-origination, neutral tied-source selection, evidence,
and synchronous scheduling rules without opinion-leader advantages or platform
mechanisms. Its directed sparse network remains fixed throughout each run.